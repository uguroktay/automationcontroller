---
- hosts: rhel_servers
  gather_facts: no
  tasks:
  - debug:
      msg: "{{ package_name }}"
  - name: Update required packages
    ansible.builtin.dnf:
      name: "{{ package_name }}"
      state: latest
      update_only: yes
      update_cache: yes
